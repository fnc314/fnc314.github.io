import { type CSSResult, css } from "@lit/reactive-element";

export const DialogStyles: CSSResult = css`
  md-dialog {
    min-width: 80dvw;
    max-width: 90dvw;
    min-height: 80dvh;
    max-height: 90dvh;
  }
`;