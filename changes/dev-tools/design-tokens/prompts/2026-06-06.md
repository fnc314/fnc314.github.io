As a senior developer, you are tasked with reviewing the @changes/dev-tools/design-tokens/implementation_plan.md and related @changes/dev-tools/design-tokens/tasks.md and @changes/dev-tools/design-tokens/follow_ups.md and finish any remaining steps towards the goal. At a high-level, we aim to:

  1. flesh out @design-tokens/ as an independent NPM module in a faux monorepo. This package produces

    - Design Tokens Community Group Format `json` file of all design tokens
    - Consumable CSS for the code housed in @src/
    - Consumable image assets
    - Packaging structure preserving layout of @src/theme and @design-tokens/src/themes

  2. use the output of @design-tokens/ to drive developer tools in the main root project. This means

    - Lint existing code to ensure and enforce exclusive use of design tokens as CSS variables
    - Break the build on violations
    - Line up tokens with appropriate properties (i.e. - forbid `--spacing-margin` tokens used for `padding` property)

  3. leverage `mise` for scripting CLI calls and managing the tools with which we "enter" the `design-tokens` project space. Use `mise` for environmental support (we version `pnpm`, which we use exclusively via `mise`). We write organized tasks as `.zsh` files under @design-tokens/.mise/tasks or @.mise/tasks, depending on the scope of the work. No task should affect a `src` directory "above" it in folder hierarchy
  4. use `pnpm` for all depdency management and include `--save-catalog-name=devDependencies -D` when calling `pnpm add` so that @design-tokens/package.json is updated to use version catalogs. This command should be invoked from project root, but ensure only the target `package.json` (i.e. @design-tokens/package.json) is modified.
  5. build steps required by @design-tokens/ are do be orchestrated via `vite`. Use @design-tokens/vite.config.ts for such endeavors. We will invoke `vite` via tasks organized under @design-tokens/.mise/tasks in `.zsh` files containing `pnpm vite ...`. Exisitng @.mise/tasks can provide good examples of this approach.

Please update @changes/dev-tools/design-tokens/implementation_plan.md and @changes/dev-tools/design-tokens/tasks.md as appropriate with the above, then perform the analysis. Once done, update
@changes/dev-tools/design-tokens/tasks.md to reflect the current status and provide a high-level guess, percentage wise, of where we stand
