<html><head></head><body>[@fnc314/com.fnc314.website](../wiki/Home) / [../../src/components/app-shell/app-shell](.....src.components.app-shell.app-shell.md) / AppShell

# Class: AppShell

Defined in: [../../src/components/app-shell/app-shell.ts:39](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L39)

## Slot

[app-nav] - Where the NavComponent is placed

## Slot

[app-content] - The place for the dynamic application content

## Hierarchy

[View Summary](../wiki/hierarchy)

### Extends

- `LitElement`

## Other

### render()

&gt; **render**(): `TemplateResult`\&lt;`1`\&gt;

Defined in: [../../src/components/app-shell/app-shell.ts:345](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L345)

Renders the application shell.
Includes navigation and content slots, global dialogs, and floating action buttons.

#### Returns

`TemplateResult`\&lt;`1`\&gt;

#### Overrides

`LitElement.render`

---

### firstUpdated()

&gt; `protected` **firstUpdated**(`_changedProperties`): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:215](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L215)

Lifecycle method called after the first update.
Initializes state from the configuration service and applies initial FAB positions.

#### Parameters

##### \_changedProperties

`PropertyValueMap`\&lt;`any`\&gt; \| [`Map`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Map)\&lt;`PropertyKey`, `unknown`\&gt;

Map of changed properties.

#### Returns

`void`

#### Overrides

`LitElement.firstUpdated`

---

### updated()

&gt; `protected` **updated**(`changedProperties`): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:230](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L230)

Lifecycle method called after every update.
Performs manual Shadow DOM adjustments for specific Material Web component styles
that cannot be easily reached via CSS Custom Properties.

#### Parameters

##### changedProperties

`PropertyValueMap`\&lt;`any`\&gt; \| [`Map`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Map)\&lt;`PropertyKey`, `unknown`\&gt;

Map of changed properties.

#### Returns

`void`

#### Overrides

`LitElement.updated`

---

### appConfigs

&gt; `private` **appConfigs**: `AppConfigs`

Defined in: [../../src/components/app-shell/app-shell.ts:126](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L126)

The current global application configuration state.

---

### \_uiModeIcon

&gt; `private` **\_uiModeIcon**: `"dark_mode"` \| `"light_mode"` \| `"routine"`

Defined in: [../../src/components/app-shell/app-shell.ts:130](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L130)

The icon associated with the current color scheme mode.

---

### configsDialog

&gt; `private` **configsDialog**: [`ConfigsDialog`](.....src.components.dialog.configs.configs-dialog.Class.ConfigsDialog.md)

Defined in: [../../src/components/app-shell/app-shell.ts:134](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L134)

Reference to the configuration dialog.

---

### fabMenu

&gt; `private` **fabMenu**: [`FabMenu`](.....src.components.fab-menu.fab-menu.Class.FabMenu.md)

Defined in: [../../src/components/app-shell/app-shell.ts:138](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L138)

Reference to the FAB menu component.

---

### settingsFabConfig

&gt; `private` **settingsFabConfig**: `FabConfig`

Defined in: [../../src/components/app-shell/app-shell.ts:142](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L142)

The configuration for the settings FAB.

---

### connectDialog

&gt; `private` **connectDialog**: [`ConnectDialog`](.....src.components.dialog.connect.connect-dialog.Class.ConnectDialog.md)

Defined in: [../../src/components/app-shell/app-shell.ts:146](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L146)

Reference to the connect dialog.

---

### connectFab

&gt; `private` **connectFab**: [`MdFab`](https://github.com/material-components/material-web/tree/main/docs/components/fab.md)

Defined in: [../../src/components/app-shell/app-shell.ts:153](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L153)

Reference to the connect FAB.
This is an MdFab component.

---

### onFabChange()

&gt; `private` **onFabChange**(`fab`, `fabConfig`): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:169](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L169)

Updates the state and DOM representation of a FAB when its configuration changes.

#### Parameters

##### fab

`"settings"` \| `"connect"`

Which FAB is being updated ('settings' or 'connect').

##### fabConfig

`FabConfig`

The new configuration settings for the FAB.

#### Returns

`void`

---

### onColorSchemeChange()

&gt; `private` **onColorSchemeChange**(`event`): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:247](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L247)

Event handler for color scheme changes.
Updates the UI icon, Material theme variables, and meta theme color.

#### Parameters

##### event

`ColorSchemeConfigChange`

The color scheme configuration change event.

#### Returns

`void`

---

### onAppConfigsChange()

&gt; `private` **onAppConfigsChange**(`event`): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:258](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L258)

Syncs the component state with the global application configuration.

#### Parameters

##### event

[`Event`](https://developer.mozilla.org/docs/Web/API/Event)

AppConfigsChange event.

#### Returns

`void`

---

### uiModeIcon()

&gt; `private` **uiModeIcon**(`colorScheme`): `"dark_mode"` \| `"light_mode"` \| `"routine"`

Defined in: [../../src/components/app-shell/app-shell.ts:292](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L292)

Maps a color scheme name to a Material icon name.

#### Parameters

##### colorScheme

`ColorSchemeConfigs`

The current color scheme configuration.

#### Returns

`"dark_mode"` \| `"light_mode"` \| `"routine"`

The string identifier for the MdIcon.

---

### \_handleDialogOpened()

&gt; `private` **\_handleDialogOpened**(): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:304](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L304)

Tracks open dialogs to manage body scroll locking.

#### Returns

`void`

---

### \_handleDialogClosed()

&gt; `private` **\_handleDialogClosed**(): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:311](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L311)

Tracks closed dialogs to manage body scroll restoration.

#### Returns

`void`

---

### \_onFabMenuItemClick()

&gt; `private` **\_onFabMenuItemClick**(`formContent`): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:324](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L324)

Handles clicks on FAB menu items.
Closes the menu and opens the configuration dialog with the requested content.

#### Parameters

##### formContent

`FormContent`

The type of configuration form to display.

#### Returns

`void`

---

### \_getFabLabel()

&gt; `private` **\_getFabLabel**(`fab`, `config`): `string`

Defined in: [../../src/components/app-shell/app-shell.ts:336](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L336)

Generates the label string for a FAB based on its configuration style.

#### Parameters

##### fab

`string`

The FAB identifier.

##### config

`FabConfig`

The FAB configuration.

#### Returns

`string`

The label string or an empty string if the style is icon-only.

## lifecycle

### connectedCallback()

&gt; **connectedCallback**(): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:262](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L262)

Invoked when the component is added to the document's DOM.

In `connectedCallback()` you should setup tasks that should only occur when
the element is connected to the document. The most common of these is
adding event listeners to nodes external to the element, like a keydown
event handler added to the window.

```ts
connectedCallback() {
  super.connectedCallback();
  addEventListener('keydown', this._handleKeydown);
}
```

Typically, anything done in `connectedCallback()` should be undone when the
element is disconnected, in `disconnectedCallback()`.

#### Returns

`void`

#### Overrides

`LitElement.connectedCallback`

---

### disconnectedCallback()

&gt; **disconnectedCallback**(): `void`

Defined in: [../../src/components/app-shell/app-shell.ts:280](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L280)

Invoked when the component is removed from the document's DOM.

This callback is the main signal to the element that it may no longer be
used. `disconnectedCallback()` should ensure that nothing is holding a
reference to the element (such as event listeners added to nodes external
to the element), so that it is free to be garbage collected.

```ts
disconnectedCallback() {
  super.disconnectedCallback();
  window.removeEventListener('keydown', this._handleKeydown);
}
```

An element may be re-connected after being disconnected.

#### Returns

`void`

#### Overrides

`LitElement.disconnectedCallback`

## styles

### styles

&gt; `static` **styles**: `CSSResult`[]

Defined in: [../../src/components/app-shell/app-shell.ts:40](https://github.com/fnc314/fnc314.github.io/blob/feature/vite/src/components/app-shell/app-shell.ts#L40)

Array of styles to apply to the element. The styles should be defined
using the css tag function, via constructible stylesheets, or
imported from native CSS module scripts.

Note on Content Security Policy:

Element styles are implemented with `<style>` tags when the browser doesn't
support adopted StyleSheets. To use such `<style>` tags with the style-src
CSP directive, the style-src value must either include 'unsafe-inline' or
`nonce-<base64-value>` with `<base64-value>` replaced be a server-generated
nonce.

To provide a nonce to use on generated `<style>` elements, set
`window.litNonce` to a server-generated nonce in your page's HTML, before
loading application code:

```html
<script>
  // Generated and unique per request:
  window.litNonce = "a1b2c3d4";
</script>
```

#### Nocollapse

#### Overrides

`LitElement.styles`
</style></body></html>
