## `packages:make-new-package`

Creates a new sub `package.json` package


- **Usage**: `packages:make-new-package <dir>`
- **Aliases**: `p:mnp`

### Arguments

#### `<dir>`

The directory for the new `package.json`