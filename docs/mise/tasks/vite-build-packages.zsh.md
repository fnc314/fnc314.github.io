## `vite:build:packages`

Runs `vite build` across `packages/`.  Mode controlled by `-p` or `--prod` flag


- **Usage**: `vite:build:packages [-p]`
- **Aliases**: `v:d:pkgs`

### Flags

#### `-p`

Builds for `production`

**Default:** `false`